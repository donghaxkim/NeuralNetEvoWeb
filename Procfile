# Flask deployment (default)
web: gunicorn app:app --bind 0.0.0.0:$PORT

# Streamlit deployment (alternative - uncomment to use)
# web: streamlit run streamlit_app.py --server.port=$PORT --server.address=0.0.0.0
